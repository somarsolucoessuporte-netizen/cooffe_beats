{
  "name": "coffee-beats-native",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build": "npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@kduma-autoid/capacitor-sunmi-printer": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
